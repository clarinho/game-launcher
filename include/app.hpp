#ifndef CAMPFIRE_APP_HPP
#define CAMPFIRE_APP_HPP

#include <string>
#include <vector>

// Runs the command-line app with top-level error handling.
int run_app(const std::vector<std::string>& args);

#endif
